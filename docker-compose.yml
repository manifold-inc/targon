services:
  nvidia-attest: 
    image: manifoldlabs/targon-attest
    restart: always
    build:
      dockerfile: Dockerfile
      context: ./nvidia-attest
      platforms:
        - linux/arm
        - linux/amd64

  targon:
    restart: always
    build:
      dockerfile: Dockerfile
      context: ./targon
    env_file: .env
