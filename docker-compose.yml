services:
  nvidia-attest: 
    image: manifoldlabs/targon-attest@latest
    restart: always

  targon:
    restart: always
    build:
      dockerfile: Dockerfile
      context: ./targon
    env_file: .env
