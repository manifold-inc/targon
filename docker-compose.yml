services:
  vllm:
    image: vllm/vllm-openai:latest
    runtime: nvidia
    command: --model mlabonne/NeuralDaredevil-7B --dtype auto
    ipc: host
    volumes:
      - hf_cache:/root/.cache/huggingface
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
volumes:
  hf_cache:
