services:
  nvidia-attest: 
    image: ghcr.io/manifold-inc/targon-nvidia-attest:latest
    restart: always

  targon:
    restart: always
    build:
      dockerfile: Dockerfile
      context: ./targon
    env_file: .env
